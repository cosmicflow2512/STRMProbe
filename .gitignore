# Downloaded static ffprobe — fetched at deploy time, never committed (~80 MB, GPL build)
/bin/ffprobe-full
bin/ffprobe-full

# Archives produced by scripts/fetch-ffprobe-full.sh
*.tar.xz
*.tar.gz

# Test scratch
/tests/.tmp/

# Logs
*.log

# Docker mod files staged from src/ by mod/stage.sh
/mod/root/

# Local Claude Code settings (not part of the project)
.claude/settings.local.json
